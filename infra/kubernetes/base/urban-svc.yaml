# Urban Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: urban-svc
  namespace: bharatsetu
  labels:
    app: urban-svc
    app.kubernetes.io/name: urban-svc
    app.kubernetes.io/component: service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: urban-svc
  template:
    metadata:
      labels:
        app: urban-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3004"
    spec:
      serviceAccountName: bharatsetu-sa
      containers:
        - name: urban-svc
          image: ${DOCKER_REGISTRY}/bharatsetu/urban-svc:${IMAGE_TAG}
          imagePullPolicy: Always
          ports:
            - containerPort: 3004
          envFrom:
            - configMapRef:
                name: bharatsetu-config
            - configMapRef:
                name: database-config
            - secretRef:
                name: bharatsetu-secrets
          env:
            - name: PORT
              value: "3004"
            - name: DATABASE_NAME
              value: "bharatsetu_urban"
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 10
            periodSeconds: 5
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
      imagePullSecrets:
        - name: docker-registry-secret
---
apiVersion: v1
kind: Service
metadata:
  name: urban-svc-service
  namespace: bharatsetu
spec:
  type: ClusterIP
  ports:
    - port: 3004
      targetPort: 3004
  selector:
    app: urban-svc
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: urban-svc-hpa
  namespace: bharatsetu
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: urban-svc
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
